version: '3.8'

services:
  # MongoDB - Primary Database
  mongodb:
    image: mongo:latest
    container_name: fcs26-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-changeme123}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE:-professional_network}
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - internal-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    security_opt:
      - no-new-privileges:true

  # Redis - Cache, Sessions, Pub/Sub
  redis:
    image: redis:latest
    container_name: fcs26-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_secret_pass} --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - internal-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis_secret_pass}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true

  # Backend API Server
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: fcs26-backend
    restart: unless-stopped
    environment:
      NODE_ENV: development
      PORT: 5000
      MONGODB_URI: mongodb://${MONGO_USERNAME:-admin}:${MONGO_PASSWORD:-changeme123}@mongodb:27017/${MONGO_DATABASE:-professional_network}?authSource=admin
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_secret_pass}@redis:6379
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://localhost}
      CLIENT_URL: ${CLIENT_URL:-https://localhost}
      RESUME_ENCRYPTION_KEY: ${RESUME_ENCRYPTION_KEY}
      CSRF_SECRET: ${CSRF_SECRET}
      COOKIE_SECRET: ${COOKIE_SECRET}
      SESSION_SECRET: ${SESSION_SECRET}
    volumes:
      - backend_uploads:/app/uploads
      - ./backend/src:/app/src
    ports:
      - "${BACKEND_PORT:-5001}:5000"
    networks:
      - internal-network
      - frontend-network
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    security_opt:
      - no-new-privileges:true

  # Frontend — production build served by nginx:alpine
  # No Vite dev server = no cold-start blank screen
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: production
      args:
        VITE_API_URL: ""
        VITE_WS_URL: ""
    container_name: fcs26-frontend
    restart: unless-stopped
    networks:
      - frontend-network
    depends_on:
      - backend
    security_opt:
      - no-new-privileges:true


  # Nginx - Reverse Proxy (HTTPS termination, DDoS protection, Clickjacking headers)
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: fcs26-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - frontend-network
    depends_on:
      - backend
      - frontend
    healthcheck:
      test: ["CMD", "curl", "-sk", "https://localhost/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    security_opt:
      - no-new-privileges:true

networks:
  internal-network:
    driver: bridge
    internal: true
  frontend-network:
    driver: bridge

volumes:
  mongodb_data:
  mongodb_config:
  redis_data:
  backend_uploads:
