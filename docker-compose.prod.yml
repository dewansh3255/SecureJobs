version: '3.8'

services:
  # MongoDB - Production
  mongodb:
    image: mongo:7.0-alpine
    container_name: fcs26-mongodb-prod
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE}
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - internal-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

  # Redis - Production
  redis:
    image: redis:7.2-alpine
    container_name: fcs26-redis-prod
    restart: always
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - internal-network
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true

  # Backend - Production
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: production
    container_name: fcs26-backend-prod
    restart: always
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    networks:
      - internal-network
      - frontend-network
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=50m
    volumes:
      - uploads_data:/app/uploads
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M

  # Frontend - Production (nginx:alpine serving pre-built static files)
  # Fixes "white screen on first load": no Vite cold-start, instant response.
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        VITE_API_URL: ""   # Falls back to /api in code → outer nginx proxies to backend
        VITE_WS_URL: ""    # Falls back to /ws in code  → outer nginx proxies to backend
    container_name: fcs26-frontend-prod
    restart: always
    networks:
      - frontend-network
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M

  # Nginx - Reverse Proxy (HTTPS termination, DDoS protection, security headers)
  nginx:
    image: nginx:alpine
    container_name: fcs26-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - frontend-network
    depends_on:
      - backend
      - frontend
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /var/cache/nginx:rw,noexec,nosuid,size=50m
      - /var/run:rw,noexec,nosuid,size=10m
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 256M

networks:
  internal-network:
    driver: bridge
    internal: true
  frontend-network:
    driver: bridge

volumes:
  mongodb_data:
  mongodb_config:
  redis_data:
  uploads_data:
